在 Neovim 中，你可以使用以下命令来移动窗口的显示范围而不移动光标：

垂直滚动：

zt: 将当前行滚动到窗口顶部。
zz: 将当前行滚动到窗口中间。
zb: 将当前行滚动到窗口底部。
水平滚动：

zl: 向右滚动窗口，移动半个屏幕宽度。
zh: 向左滚动窗口，移动半个屏幕宽度。
zL: 向右滚动窗口，移动一个完整的屏幕宽度。
zH: 向左滚动窗口，移动一个完整的屏幕宽度。

根据你的 lua/modules/configs/ui/neoscroll.lua 文件，neoscroll.nvim 插件配置了以下垂直滚动键位：

<C-u>
<C-d>
<C-b>
<C-f>
<C-y>
<C-e>
zt
zz
zb
这些键位会使用 neoscroll.nvim 的平滑滚动动画。对于水平滚动，你可以继续使用 Neovim 的内置命令 zh, zl, zH, zL，但这些命令不会有 neoscroll.nvim 的平滑效果，除非你在配置中为它们添加了相应的映射。

vimtutor

高亮跳转 - <C-]>, `<C-T>`

g;: 跳转到你最近编辑过的位置。重复使用会按时间倒序遍历编辑历史。
` (两个反引号): 跳转到你上次离开缓冲区时的确切位置（行和列）。

LSP 快速修复

ga
g[

分割查看
:vert: 强制进行垂直分割。
:sbuffer: 这个命令专门用于在新的分割窗口中打开一个已存在的缓冲区，参数可以是缓冲区的编号或名称。


搜索使用粘贴 Ctrl + r + 0/" 使用寄存器

y 复制到匿名存储器"和0号存储器
:%s/Ctrl + r + "/g


按键组合,功能描述
Ctrl + w 然后 h,切换到 左边 的窗口
Ctrl + w 然后 j,切换到 下边 的窗口
Ctrl + w 然后 k,切换到 上边 的窗口
Ctrl + w 然后 l,切换到 右边 的窗口
Ctrl + w 然后 w,循环切换到下一个窗口 (Cycle)
Ctrl + w 然后 p,切换到 上一个窗口


遇到 toggleterm 卡死可以焦点在该窗口上使用 :q!

| 命令 | 含义 | 场景 |
| :--- | :--- | :--- |
| **`dgn`** | **Delete** Go Next (删除下一个匹配项) | 配合 `.` 使用，快速剔除不需要的搜索词。 |
| **`ygn`** | **Yank** Go Next (复制下一个匹配项) | 想要把搜索到的词复制到剪贴板时。 |
| **`gUgn`** | **Uppercase** Go Next (大写下一个匹配项) | 快速把搜索到的变量名转为大写 (配合 `.` )。 |
| **`cgN`** | Change Go **Previous** (修改上一个) | `cgn` 的反向版。如果你不小心按过头了，可以用这个改回去。 |

| 命令 | 含义 | 连击技巧 (配合 `.`) |
| --- | --- | --- |
| **`A`** | 在**行尾**附加 (Append at End) | 在一行行尾加分号 -> `Esc` -> `j` (下移) -> `.` -> `j` -> `.` |
| **`I`** | 在**行首**插入 (Insert at Start) | 在行首加注释符 -> `Esc` -> `j` -> `.` |
| **`C`** | 修改从光标到行尾的内容 | 删除光标后所有内容并重写。 |
| **`S`** (或 `cc`) | 清空整行并重写 | 快速重写整行代码。 |


## 分屏操作

telescope
Ctrl + x 水平
Ctrl + v

:vs
:vsp

## 关闭 windows

:only
ctrl + w o

:tabonly
to

tabs

| 操作 | Buffer 相关 | Tab 相关 |
| --- | --- | --- |
| **新建** | `:e filename` | `:tabnew` |
| **切换到下一个** | `:bn` | `gt` |
| **切换到上一个** | `:bp` | `gT` |
| **关闭当前** | `:bd` | `:tabc` |
| **查看所有** | `:ls` | `:tabs` |


| 方式 | 你的操作 | 脑力消耗 | 适用场景 |
| --- | --- | --- | --- |
| **显示标签栏** | 眼睛在顶部左右扫描 -> 鼠标点或多次按 `gt` | 低，但浪费眼力且慢 | 刚从 VS Code 迁移，文件少时 |
| **Telescope 搜索** | 快捷键 -> 输入 `idx` (文件名简写) -> 回车 | **极低** | 文件很多，记得文件名但不记得在哪 |
| **Ctrl + ^** | 一个组合键（瞬间回到上个 Buffer） | **零** | 在两个关联文件（如代码和测试）间反复横跳 |
| **Jumps (Ctrl+o/i)** | `Ctrl + o` (后退) / `Ctrl + i` (前进) | **零** | 追溯代码调用链，想回到刚才看的地方 |
| **Global Marks** | `m` + `A-Z` (标记) -> `'` + `A-Z` (跳转) | **中** (需记哪个字母对应哪个文件) | 跨项目、跨 Tab 的**绝对坐标**跳转 |
| **Harpoon 插件** | `leader + 1/2/3/4` | **低** (固定顺位记忆) | 核心开发中，锁死最重要的 4 个文件 |

Export to Sheets

---

### 详细拆解：Jumps 与 Marks 的威力

这两个功能让你不需要看 Tab 栏，就能在不同的 Buffer 和位置之间“闪现”。

#### 1\. Jumps (光标的“后悔药”)

Neovim 会自动记录你光标的大跨度跳转（比如从一个文件跳到另一个文件，或者搜索后的大距离移动）。

- **`Ctrl + o` (Outer/Older)**：回到上一个位置（可以跨文件）。
    
- **`Ctrl + i` (Inner/Newer)**：跳到下一个位置。
    
- **优势**：这完全不需要动脑。你只管往深处看代码，看完了想回去，连按几下 `Ctrl + o` 就回到了起点。
    

#### 2\. Marks (空间传送门)

Marks 分为“小写字母”和“大写字母”，**大写字母 Marks 是全局的**，这是不切 Tab 也能跨文件的关键：

- **如何标记**：在 `main.lua` 第 50 行输入 `mM`（m 代表 mark，M 是你给它起的名）。
    
- **如何跳转**：你在任何地方（哪怕在另一个 Tab、另一个项目里），输入 `'M` (单引号 + M)，它会立刻带你回到 `main.lua` 的第 50 行。
    
- **优势**：非常适合那种“我一会儿还要回来改这里”的临时锚点。
    

---

### 建议的工作流组合

如果你决定尝试\*\*“无标签栏（No-Tabline）”\*\*编辑，我建议你同时形成这三个肌肉记忆：

1. **频繁跳**：用 `Ctrl + ^` 在两个文件间切换（最常用）。
    
2. **深度跳**：用 `Ctrl + o` 回到刚才看代码的地方。
    
3. **精准跳**：用 `Telescope` (`:Telescope buffers`) 找那个你半小时前打开过的文件。
    

**你想试试把 `Ctrl + ^` 这个有点别扭的按键映射到一个更爽的键位上吗（比如映射到 `Backspace` 或 `Tab`）？我可以给你代码。**

![profile picture](https://lh3.googleusercontent.com/a/ACg8ocLvskWujLqGIn8s6AOLBXhYb2n3fobKHEEf5oZPuTWNfDxGPBY=s64-c-v1-rj)

---

选择中调整

o

O 是 Ctrl + V 块选择水平跳转
